[project]
name = "reqs-builder"
version = "0.1.0"
description = "A documentation build tool"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
    "pyright",
    "pytest",
    "pytest-cov",
    "ruff>=0.15.7",
]

[tool.pyright]
typeCheckingMode = "strict"
