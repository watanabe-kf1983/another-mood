# Built-in prose schema — validates the structure produced by Markdown parsing.
#
# Markdown files in contents_dir are converted to this structure by the
# Normalizer's prose parser.  This schema is merged with the user's
# schema.yaml so that all content files are validated uniformly.
#
# See: dev-docs/contents/design/normalizer/markdown-parser-spec.md

type: object
properties:
  prose:
    type: array
    items:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        body:
          type: object
          properties:
            mime_type:
              type: string
            content:
              type: string
          required: [mime_type, content]
      required: [id, body]
