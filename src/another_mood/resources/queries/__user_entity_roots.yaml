# Root entities surfaced in the built-in __root template's Entity list
# (excludes catalog-internal entities whose ids start with "__").  The
# per-entity __entity_defs / __entity_data queries cover the full root
# entity set directly so the hidden built-in entities still get meta pages.
__user_entity_roots:
  from: __definition.entities
  where:
    view: false
    parent_entity: { exists: false }
    not:
      id: { startswith: __ }
  select:
    - item: id
    - item: builtin
    - item: item_type
  sort: { by: builtin }
