# Normalizer

## Proposals

### バイナリファイルの正規化 (H1, H2)

`contents_dir` には YAML・Markdown のほかにバイナリファイル（PNG, JPG 等）も配置される想定。バイナリファイルの正規化における扱い（パス解決、Composer への受け渡し等）は未決定。Phase 8 タスク [H1, H2](../../../tasks.md)（仕様確定 + 実装）。

### Unique 制約 (D8, D9)

追加の Unique 制約（id 以外のフィールドに対する一意性）の宣言。Phase 10 タスク [D8, D9](../../../tasks.md)。
