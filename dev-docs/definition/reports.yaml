# Report output configuration. See:
# dev-docs/contents/design/generator/paging-spec.md
#
# `file_per:` lists ObjectType IDs whose nodes are split into separate
# files. An empty list keeps everything in a single index.md.
file_per: []
