# テーブル: {{ 名前 }} (`{{ id | safe }}`)

{{ 説明 }}

## 列

| 列名 | 型 | 必須 | 主キー | 参照 |
|------|----|------|--------|------|
{% for c in 列 -%}
| {{ c.名前 }} | `{{ c.型 | safe }}` | {% if c.必須 %}○{% endif %} | {% if c.主キー %}○{% endif %} | {% if c.参照 %}`{{ c.参照.テーブル | safe }}.{{ c.参照.列 | safe }}`{% endif %} |
{% endfor %}
