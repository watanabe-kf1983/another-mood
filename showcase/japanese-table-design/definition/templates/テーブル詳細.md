# テーブル: {{ 名前 }} ({{ code_inline(id) }})

{{ 説明 }}

## 列

| 列名 | 型 | 必須 | 主キー | 参照 |
|------|----|------|--------|------|
{% for c in 列 %}
| {{ c.名前 }} | {{ code_inline(c.型) }} | {% if c.必須 %}○{% endif %} | {% if c.主キー %}○{% endif %} | {% if c.参照 %}{{ code_inline(c.参照.テーブル ~ "." ~ c.参照.列) }}{% endif %} |
{% endfor %}
