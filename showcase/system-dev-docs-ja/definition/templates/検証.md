{#- 要求仕様書 4 章「検証」の本体。3 章と同じ二つの view (ユースケース記述 /
    要求種別) を回し、各行に載った 要求 を 検証方法 / 適合基準 の列で描き直す。
    3 章の行を render し直すと web edition で同じ出力パスに二頁が衝突し、要求の
    配列を render すると anchor が重複する。そのため節は for ループで並べ、表は
    include で描き、ID 列は 3 章のコピー (要求の home) へリンクする。4 章は要求の
    コピーを持たず、どの edition でも分割しない。 -#}
#### 機能要求の検証

「要求」章の機能要求の節と同じ順序で、ユースケースごとにその要求の検証方法と適合基準を示す。

{% for uc in ユースケース記述 %}
##### {{ uc | link(uc.名前) }} ({{ uc.id }})

{% set 要求 = uc.要求 %}
{% include "検証一覧.md" %}

{% endfor %}
#### 非機能要求の検証

「要求」章の非機能要求の節と同じ順序で、種別ごとにその要求の検証方法と適合基準を示す。

{% for k in 要求種別 %}
##### {{ k | link }}

{% set 要求 = k.要求 %}
{% include "検証一覧.md" %}

{% endfor %}
