## 要求仕様書

小規模書店向け在庫管理システムの要求仕様書。用語は{{ 用語 | link("用語集") }}に定義する。

### はじめに

本章では、本書の目的と対象範囲、および製品の概観を述べる。

{{ node(path="/prose/要求仕様書/はじめに") | render("prose.md") | under_heading("###") }}

{{ 用語 | render("用語集.md") | under_heading("##") }}

### 要求

本章では、本システムへの要求を述べる。機能要求はユースケースごとに、外部インタフェース・品質・設計制約の要求は種別ごとに節を分ける。

### 検証

本章では、「要求」章の各要求に対する検証方法と適合基準を、同じ順序で示す。

### 付録

本章では、本書の前提と依存事項を挙げる。

{{ node(path="/prose/要求仕様書/付録") | render("prose.md") | under_heading("###") }}
