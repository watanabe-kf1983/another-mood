## 要求仕様書

{{ システム.説明 }}の要求仕様書。用語は{{ 用語 | link("用語集") }}に定義する。

### はじめに

本書の目的と対象範囲、および製品の概観を述べる。

{{ node(path="/prose/要求仕様書/はじめに") | render("prose.md") | under_heading("###") }}

{{ ユースケース | render("主要機能.md") | under_heading("###") }}

{{ アクター_with_ユースケース | render("利用者と外部システム.md") | under_heading("###") }}

{{ node(path="/prose/要求仕様書/制約") | render("prose.md") | under_heading("###") }}

{{ 用語 | render("用語集.md") | under_heading("##") }}

### 要求

本システムへの要求を、機能要求はユースケースごとに、非機能要求 (外部インタフェース・品質・設計制約) は種別ごとに示す。

#### 機能要求

ユースケースごとに、その記述 (アクター、事前条件と事後条件、基本シナリオと例外) と、そのユースケースに属する要求を示す。ユースケースの並びは{{ ユースケース | link("主要機能") }}と同じく業務の流れの順である。

{% for uc in ユースケース記述 %}
- {{ uc | link(uc.名前) }} ({{ uc.id }})
{% endfor %}

{% for uc in ユースケース記述 %}
{{ uc | render("ユースケース記述.md") | under_heading("####") -}}
{% endfor %}

#### 非機能要求

ユースケースに属さない要求を、種別 (外部インタフェース / 品質 / 設計制約) ごとに示す。

{% for k in 要求種別 %}
- {{ k | link(k.id) }}
{% endfor %}

{% for k in 要求種別 %}
{{ k | render("要求種別.md") | under_heading("####") -}}
{% endfor %}

### 検証

「要求」章の各要求に対する検証方法と適合基準を、機能要求はユースケースごとに、非機能要求は種別ごとに、要求と同じ順序で示す。

{% include "検証.md" %}

### 付録

本書の前提と依存事項を挙げ、本システムが支える書店の業務の全体像を{{ 分解図 | link("業務モデル") }}として示す。

{{ node(path="/prose/要求仕様書/付録") | render("prose.md") | under_heading("###") }}

{{ 分解図 | render("業務モデル.md") | under_heading("###") }}
