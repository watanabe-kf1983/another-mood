## 要求仕様書

小規模書店向け在庫管理システムの要求仕様書。用語は{{ 用語 | link("用語集") }}に定義する。

### はじめに

本章では、本書の目的と対象範囲、および製品の概観を述べる。

{{ node(path="/prose/要求仕様書/はじめに") | render("prose.md") | under_heading("###") }}

{{ ユースケース | render("主要機能.md") | under_heading("###") }}

{{ アクター_with_ユースケース | render("利用者と外部システム.md") | under_heading("###") }}

{{ node(path="/prose/要求仕様書/制約") | render("prose.md") | under_heading("###") }}

{{ 用語 | render("用語集.md") | under_heading("##") }}

### 要求

本章では、本システムへの要求を述べる。機能要求はユースケースごとに、非機能要求 (外部インタフェース・品質・設計制約) は種別ごとに節を分ける。

#### 機能要求

ユースケースごとに、その記述 (アクター、事前条件と事後条件、基本シナリオと例外) と、そのユースケースに属する要求を示す。ユースケースの並びは{{ ユースケース | link("主要機能") }}と同じく業務の流れの順である。要求の理由は、要求文だけでは必要性が読み取れないものに付す。

{% for uc in ユースケース記述 %}
- {{ uc | link(uc.名前) }} ({{ uc.id }})
{% endfor %}

{% for uc in ユースケース記述 %}
{{ uc | render("ユースケース記述.md") | under_heading("####") -}}
{% endfor %}

#### 非機能要求

ユースケースに属さない要求を、種別 (外部インタフェース / 品質 / 設計制約) ごとに示す。

{% for k in 要求種別 %}
- {{ k | link(k.id) }}
{% endfor %}

{% for k in 要求種別 %}
{{ k | render("要求種別.md") | under_heading("####") -}}
{% endfor %}

### 検証

本章では、「要求」章の各要求に対する検証方法と適合基準を、同じ順序で示す。

### 付録

本章では、本書の前提と依存事項を挙げる。

{{ node(path="/prose/要求仕様書/付録") | render("prose.md") | under_heading("###") }}
