## 設計書

小規模書店向け在庫管理システムの基本設計書。本システムの境界は{{ コンテキスト | link("コンテキスト") }}の章に示し、用語は要求仕様書の{{ 用語 | link("用語集") }}に従う。

### はじめに

本章では、本書の目的と対象範囲、記法と規約、および{{ 設計判断 | link("設計方針") }}を述べる。

{{ node(path="/prose/設計書/はじめに") | render("prose.md") | under_heading("###") }}

{{ 設計判断 | render("設計方針.md") | under_heading("###") }}

{{ コンテキスト | render("コンテキスト.md") | under_heading("##") }}

### データ設計

本章では、本システムが扱うデータを{{ エンティティ | link("論理データモデル") }}として示し、どのユースケースがどのエンティティを読み書きするかを CRUD マトリクスで示す。

{{ エンティティ | render("論理データモデル.md") | under_heading("###") }}

### 画面設計

本章では、画面の一覧、画面間の遷移、および各画面の定義を示す。
