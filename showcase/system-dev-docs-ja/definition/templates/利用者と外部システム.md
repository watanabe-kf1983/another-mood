# 利用者

本システムを直接操作する人の役割と、各役割が関わる機能を示す。

| 利用者 | 役割 | 関わる機能 |
|--------|------|------------|
{% for a in this if a.種別 == "人" %}
| {{ node("アクター", a.id) | anchor }}{{ a.名前 }} | {{ a.説明 | in_cell }} | {% for uc in a.ユースケース %}{{ node("ユースケース", uc.id) | link(uc.名前) }}{% if not loop.last %}、{% endif %}{% endfor %} |
{% endfor %}

# 外部システム

本システムと連携する外部システムと、各システムが関わる機能を示す。

| 外部システム | 役割 | 関わる機能 |
|--------------|------|------------|
{% for a in this if a.種別 == "外部システム" %}
| {{ node("アクター", a.id) | anchor }}{{ a.名前 }} | {{ a.説明 | in_cell }} | {% for uc in a.ユースケース %}{{ node("ユースケース", uc.id) | link(uc.名前) }}{% if not loop.last %}、{% endif %}{% endfor %} |
{% endfor %}
