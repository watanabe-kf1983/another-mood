{#- 設計書「設計方針」節。主題は 設計判断 のコレクションで、判断ごとに小節
    (見出し = ID + 題名、本文 = 判断、小見出し 理由 / 根拠要求) を置く。
    根拠要求は ID しか持たないので、node("要求", ID) でレコードを引いて題名を
    添え、リンクは要求の home (要求仕様書 3 章の view 行の配下のコピー) に張る。
    home は機能要求ならユースケース記述の行、それ以外なら要求種別の行の配下で、
    要求.ユースケース の有無で使い分ける。entity 要求 のノードはどこにも render
    されないので、node("要求", ID) | link では着地しない。 -#}
# 設計方針

本システムの設計判断を、その理由と根拠になった要求とともに示す。根拠要求は要求仕様書の要求を ID と題名で指し、要求に由来しない判断 (技術上の慣行など) では持たない。

{% for d in this %}
{{ d | anchor }}
## {{ d.id }} {{ d.題名 }}

{{ d.判断 }}

### 理由

{{ d.理由 }}
{% if d.根拠要求 %}

### 根拠要求

{% for b in d.根拠要求 %}
{% set r = node("要求", b.ID) %}
{% if r.ユースケース %}
{% set home = node("ユースケース記述", r.ユースケース, "要求", r.id) %}
{% else %}
{% set home = node("要求種別", r.種別, "要求", r.id) %}
{% endif %}
- {{ home | link(r.id) }} {{ r.題名 }}
{% endfor %}
{% endif %}

{% endfor %}
