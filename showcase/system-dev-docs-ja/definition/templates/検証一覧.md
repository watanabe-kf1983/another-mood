{#- 要求の検証方法と適合基準の表。要求一覧.md と同じ 要求 配列を、4 章の列で
    描く。render ではなく include で使う (呼び出し側が 要求 を set する) ので、
    主題は this ではなく変数 要求。 -#}
| ID | 要求 | 検証方法 | 適合基準 |
|----|------|----------|----------|
{% for r in 要求 %}
| {{ r | link(r.id) }} | {{ r.題名 }} | {{ r.検証方法 }} | {{ r.適合基準 | in_cell }} |
{% endfor %}
