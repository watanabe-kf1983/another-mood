{#- 1 画面の定義。主題は 画面 の 1 件で、web edition では 1 ページ、book edition では
    画面定義の節の中にインラインで出る。主題の anchor は render が自動で付けるので
    書かない。形はユースケース記述と同じ (概要 → 項目表 → 小見出しの表)。

    入出力項目.参照先.属性 は素の文字列で x-ref が効かない (x-ref はエンティティの ID
    しか指せない) ので、エンティティ定義と同じく参照先のレコードを node() で引き、
    その 属性[] に同じ名前があるかを突合して、無ければ表に印を出す。遷移先 は x-ref
    なので画面の頁へそのままリンクする。 -#}
# {{ this.名前 }} ({{ this.id }})

{{ this.説明 }}

| 項目 | 内容 |
|------|------|
| 分類 | {{ this.分類 }} |
| 関連ユースケース | {% for u in this.ユースケース %}{{ node("ユースケース記述", u.ID) | link(u.ID) }} {{ node("ユースケース", u.ID).名前 }}{% if not loop.last %}、{% endif %}{% else %}(なし){% endfor %} |

## 入出力項目

{% if this.入出力項目 %}
| 項目 | 入出力 | 参照先 | 説明 |
|------|--------|--------|------|
{% for i in this.入出力項目 %}
{% set dst = node("エンティティ", i.参照先.エンティティ) if i.参照先 else none %}
| {{ i.名前 }} | {{ i.入出力 }} | {% if i.参照先 %}{{ dst | link(dst.名前) }}.{{ i.参照先.属性 }}{% if i.参照先.属性 not in dst.属性 | map(attribute="名前") | list %} (参照先に無い属性名){% endif %}{% endif %} | {{ i.説明 | in_cell }} |
{% endfor %}
{% else %}
入出力項目は無い。
{% endif %}

## アクション

| アクション | 説明 | 遷移先 |
|------------|------|--------|
{% for a in this.アクション %}
| {{ a.名前 }} | {{ a.説明 | in_cell }} | {% for t in a.遷移 %}{% set dst = node("画面", t.遷移先) %}{{ dst | link(dst.名前) }}{% if t.条件 %} ({{ t.条件 }}){% endif %}{% if not loop.last %}、{% endif %}{% endfor %} |
{% endfor %}
