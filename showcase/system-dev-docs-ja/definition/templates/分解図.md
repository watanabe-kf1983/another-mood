{#- 付録「業務モデル」の分解図 1 枚。主題は 分解図 ビューの 1 行 (id = 親プロセスの
    ID、子 = 子プロセスの並び) で、web edition では 1 ページになる。

    図に描くフローは、元か先の少なくとも一方が子プロセスであるフロー。その相手が
    子プロセスでなければ図の外の相手で、外部エンティティ・データストア・他の図の
    プロセスをそのまま描く (Mermaid はノードの再宣言を許すので、線ごとに両端を
    宣言して重複を気にしない)。フローの表は図と同じ選び方で、上位のフロー (親)
    の列がバランシングの対応を示す。 -#}
{% import "業務マクロ.md" as m %}
{% set 親 = node("プロセス", this.id) %}
{% set ids = this.子 | map(attribute="id") | list %}
# 図 {{ m.番号(親) }}: {{ 親.名前 }}の分解

{{ 親.名前 }}は、{% for c in this.子 %}{{ m.表示名(c) }}{% if not loop.last %}、{% endif %}{% endfor %} の {{ this.子 | length }} プロセスに分解する。

```mermaid
flowchart LR
{% for c in this.子 %}
  {{ c.id | safe }}(("{{ m.表示名(c) | safe }}"))
{% endfor %}
{% for f in node("フロー") if f.元.プロセス in ids or f.先.プロセス in ids %}
  {{ m.端ノード(f.元) | safe }} -->|"{{ f.名前 | safe }}"| {{ m.端ノード(f.先) | safe }}
{% endfor %}
```

| ID | フロー | 元 | 先 | 上位のフロー | 説明 |
|----|--------|----|----|--------------|------|
{% for f in node("フロー") if f.元.プロセス in ids or f.先.プロセス in ids %}
| {{ f.id }} | {{ f.名前 }} | {{ m.端名(f.元) }} | {{ m.端名(f.先) }} | {% if f.親 %}{{ f.親 }} {{ node("フロー", f.親).名前 }}{% endif %} | {{ f.説明 | in_cell }} |
{% endfor %}
