{#- 要求仕様書 付録「業務モデル」。主題は 分解図 ビュー (分解されているプロセス
    ごとに 1 行) で、web edition ではこの節 (プロセス階層 + 一覧) が 1 ページ、
    各分解図 (分解図.md) がそれぞれ 1 ページになる。プロセス / データストア /
    外部エンティティ の一覧は主題の外なので node() で引く。

    コンテキスト図 (レベル 0) は 1 章の業務コンテキスト (業務コンテキスト.md) に
    置き、付録はレベル 1 以下を持つ。プロセス階層の表は プロセス の記述順 (深さ
    優先) をそのまま並べ、番号はマクロで導く。葉の名前は 1 章のシステム化範囲の
    行 (プロセス の home) へリンクし、分解されるプロセスは分解図へリンクする。Mermaid の図形は DeMarco / Yourdon 寄り: プロセス = 円、外部エンティティ
    = 矩形、データストア = 円筒 (平行線は flowchart に無い)。角丸 (Gane-Sarson の
    プロセス) は矩形と見分けにくいので採らない。記法の凡例は
    置かない (他章と同じ)。図の中からはリンクを張れないので、分解図への導線は
    プロセス階層の表に置く。

    バランシング検査は分解図ごとに、境界を跨ぐフロー (片端だけが図の中) が
    上位のフローを持ちその両端が対応すること、図の中で閉じるフロー (両端が
    図の中) が上位のフローを持たないこと、親プロセスの入出力 (上位の図で親
    プロセスを端に持つフロー) がすべて図の中のフローに束ねられていることを
    確かめる。「図の中」の判定は分解図の描画と同じ規則 (端のプロセスが子) で、
    レベルをまたぐプロセス間のフローは描画と同じく想定しない。データストアは
    業務の内側にあってコンテキスト図には現れず、下位の図で初めて描かれることも
    ある (構造化分析の規則: データストアは複数のプロセスが共有する最下位の
    レベルで描く) ので、データストアとのフローが上位のフローを要するのは、その
    データストアが上位の図に描かれている (親プロセスの兄弟がそれに触れている)
    ときに限る。フローの元 / 先
    が 1 つに定まることもここで確かめる (スキーマでは「ちょうど 1 つ」を
    言えない)。不一致の行は {% set %} ブロックに溜め、空なら一致の一文を出す。
    ブロックの中の substitution は既定のエスケープに任せ、出すときに | safe で
    二重を避ける。 -#}
{% import "業務マクロ.md" as m %}
# 業務モデル

本システムが支える書店の業務の全体を、階層化したデータフロー図 (DFD) で示す。図の境界は書店の業務であって本システムではない。本システムが担うのは業務の一部で、その範囲は{{ node("プロセス") | link("システム化範囲") }}に、境界は設計書のコンテキスト章が描く。業務全体を 1 つのプロセスとして外部との入出力を示すコンテキスト図は{{ node("プロセス") | link("業務コンテキスト") }}にあり、ここではプロセスを段階的に分解して、各プロセスとデータストア・外部エンティティの間のデータの流れを分解図として示す。

## プロセス階層

| 番号 | プロセス | 説明 | 分解図 |
|------|----------|------|--------|
{% for p in node("プロセス") %}
{% set 分解 = node("分解図") | selectattr("id", "equalto", p.id) | list %}
| {% if p.親 %}{{ m.番号(p) }}{% endif %} | {% if p.親 and not 分解 %}{{ p | link(p.名前) }}{% else %}{{ p.名前 }}{% endif %} | {{ p.説明 | in_cell }} | {% for d in 分解 %}{{ d | link("図 " ~ m.番号(p)) }}{% endfor %} |
{% endfor %}

番号の無い行が業務全体で、その分解が図 0 である。番号を持つプロセスのうち分解図を持たないものが葉で、業務の単位としてシステム化範囲とユースケース記述が参照する (名前がその行へのリンク)。

## データストア

| データストア | 説明 |
|--------------|------|
{% for s in node("データストア") %}
| {{ s.名前 }} | {{ s.説明 | in_cell }} |
{% endfor %}

## 外部エンティティ

| 外部エンティティ | 説明 |
|------------------|------|
{% for e in node("外部エンティティ") %}
| {{ e.名前 }} | {{ e.説明 | in_cell }} |
{% endfor %}

外部エンティティは業務の境界の外側の相手であり、本システムのアクター (店員、店主、決済サービス) とは境界が違う。店員と店主は業務の内側にいてプロセスを担い、決済サービスは会計の道具であって業務の相手ではない。

## 分解図

分解されているプロセスごとに、その子プロセスと、子プロセスが図の外の相手 (外部エンティティ、データストア、他の図のプロセス) と交わすフローを示す。図の外の相手との線は、上位の図で親プロセスの入出力として描かれている線を束ねたもので、各図のフローの表がその対応 (上位のフロー) を持つ。対象は{% for d in this %}{{ d | link("図 " ~ m.番号(node("プロセス", d.id))) }}{% if not loop.last %}、{% endif %}{% endfor %}の {{ this | length }} 枚で、並びはプロセス階層と同じである。

{% for d in this %}
{{ d | render("分解図.md") | under_heading("###") -}}
{% endfor %}

## バランシング検査

分解図の入出力は、上位の図で親プロセスの入出力として描かれたフローと一致していなければならない (バランシング)。分解図ごとに、境界を跨ぐフローが上位のフローを持ちその両端が対応すること、図の中で閉じるフローが上位のフローを持たないこと、親プロセスの入出力がすべて図の中のフローに束ねられていることを確かめる。データストアは業務の内側にあってコンテキスト図には現れないので、データストアとのフローは、そのデータストアが上位の図に描かれているときに限り上位のフローを要する。あわせて、フローの元と先がそれぞれ 1 つの相手に定まることを確かめる。

{% set 不一致 %}
{% for f in node("フロー") %}
{% for x in [f.元, f.先] %}
{% if (1 if x.プロセス else 0) + (1 if x.データストア else 0) + (1 if x.外部エンティティ else 0) != 1 %}
| | {{ f.id }} {{ f.名前 }} | {{ "元" if loop.index == 1 else "先" }}が 1 つの相手に定まらない |
{% endif %}
{% endfor %}
{% endfor %}
{% for d in this %}
{% set 図 = d | link("図 " ~ m.番号(node("プロセス", d.id))) %}
{% set ids = d.子 | map(attribute="id") | list %}
{% set 上親 = node("プロセス", d.id).親 or "" %}
{% for g in node("フロー") if g.元.プロセス == d.id %}
{% for f in node("フロー") if f.親 == g.id and f.元.プロセス in ids %}{% else %}
| {{ 図 }} | {{ g.id }} {{ g.名前 }} | 親プロセスの出力だが、図の中のどのフローにも束ねられていない |
{% endfor %}
{% endfor %}
{% for g in node("フロー") if g.先.プロセス == d.id %}
{% for f in node("フロー") if f.親 == g.id and f.先.プロセス in ids %}{% else %}
| {{ 図 }} | {{ g.id }} {{ g.名前 }} | 親プロセスの入力だが、図の中のどのフローにも束ねられていない |
{% endfor %}
{% endfor %}
{% for f in node("フロー") if f.元.プロセス in ids or f.先.プロセス in ids %}
{% set 元内 = f.元.プロセス in ids %}
{% set 先内 = f.先.プロセス in ids %}
{% if 元内 and 先内 %}
{% if f.親 %}
| {{ 図 }} | {{ f.id }} {{ f.名前 }} | 図の中で閉じるフローだが、上位のフロー {{ f.親 }} を持つ |
{% endif %}
{% elif not f.親 %}
{% set S = f.先.データストア if 元内 else f.元.データストア %}
{% if S %}
{% for g in node("フロー") if (g.元.データストア == S or g.先.データストア == S) and ((g.元.プロセス and (node("プロセス", g.元.プロセス).親 or "") == 上親) or (g.先.プロセス and (node("プロセス", g.先.プロセス).親 or "") == 上親)) %}
{% if loop.first %}
| {{ 図 }} | {{ f.id }} {{ f.名前 }} | 上位の図に {{ node("データストア", S).名前 }} が描かれているが、上位のフローが無い |
{% endif %}
{% endfor %}
{% else %}
| {{ 図 }} | {{ f.id }} {{ f.名前 }} | 図の境界を跨ぐが、上位のフローが無い |
{% endif %}
{% else %}
{% set g = node("フロー", f.親) %}
{% set 対応 = (m.端(g.元) == "プロセス:" ~ d.id and m.端(g.先) in [m.端(f.先), m.上位端(f.先)]) if 元内 else (m.端(g.先) == "プロセス:" ~ d.id and m.端(g.元) in [m.端(f.元), m.上位端(f.元)]) %}
{% if not 対応 %}
| {{ 図 }} | {{ f.id }} {{ f.名前 }} | 上位のフロー {{ g.id }} {{ g.名前 }} ({{ m.端名(g.元) }} → {{ m.端名(g.先) }}) と両端が対応しない |
{% endif %}
{% endif %}
{% endfor %}
{% endfor %}
{% endset %}
{% if 不一致 | trim %}
| 図 | フロー | 不一致 |
|----|--------|--------|
{{ 不一致 | trim | safe }}
{% else %}
{{ this | length }} 枚の分解図すべてで上位と下位の入出力は一致しており、元と先の定まらないフローは無い。
{% endif %}
