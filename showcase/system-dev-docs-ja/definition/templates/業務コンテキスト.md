{#- 要求仕様書 1 章「業務コンテキスト」と「システム化範囲」。主題は プロセス の
    コレクションで、葉プロセスの home はここ (システム化範囲の表の行) になる。
    付録の業務モデルは 分解図 ビューを主題にするので、プロセス そのものを render
    するのはこの節が初めてで、章専用の view は要らない。

    コンテキスト図 (レベル 0) は 親 を持たないプロセス (業務全体) と、それを端に
    持つフローから描く。付録がレベル 1 以下 (分解図) を持つので、ここにはレベル 0
    だけを置く。図形は付録と同じ (業務マクロ.md の 端ノード)。

    システム化範囲の表は葉プロセス (子を持たないプロセス) を行にし、各葉を支える
    ユースケース (ユースケース.業務プロセス がその葉を指すもの) を逆引きで並べる。
    ユースケースの無い葉は手作業のまま残る業務で、伏せずに (手作業) と示す。
    葉の判定は「自分を 親 に持つプロセスが無い」で、view の where では書けないので
    テンプレートで絞る。 -#}
{% import "業務マクロ.md" as m %}
# 業務コンテキスト

本システムが支える書店の業務と、業務の外側の相手 (外部エンティティ) との間のデータの流れを示す。図の境界は書店の業務であって本システムではない。業務の内側の分解は{{ node("分解図") | link("業務モデル") }}に示す。

```mermaid
flowchart LR
{% for r in this if not r.親 %}
  {{ r.id | safe }}(("{{ r.名前 | safe }}"))
{% for f in node("フロー") if f.元.プロセス == r.id or f.先.プロセス == r.id %}
  {{ m.端ノード(f.元) | safe }} -->|"{{ f.名前 | safe }}"| {{ m.端ノード(f.先) | safe }}
{% endfor %}
{% endfor %}
```

# システム化範囲

業務の単位 (業務モデルの葉プロセス) ごとに、それを支える本システムの機能 (ユースケース) を示す。機能の無い業務は本システムの範囲外で、手作業のまま残る。

| 番号 | 業務 | 説明 | 機能 |
|------|------|------|------|
{% for p in this if p.親 and not (this | selectattr("親", "equalto", p.id) | list) %}
| {{ p | anchor }}{{ m.番号(p) }} | {{ p.名前 }} | {{ p.説明 | in_cell }} | {% for uc in node("ユースケース") if p.id in uc.業務プロセス | default([]) | map(attribute="ID") | list %}{{ node("ユースケース記述", uc.id) | link(uc.名前) }}{% if not loop.last %}、{% endif %}{% else %}(手作業){% endfor %} |
{% endfor %}
